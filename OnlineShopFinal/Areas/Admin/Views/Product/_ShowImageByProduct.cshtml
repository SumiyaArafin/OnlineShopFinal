@using Microsoft.AspNetCore.Http
@model IEnumerable<OnlineShopFinal.ViewModel.ProductImageListViewModel>




<h2 style="text-align:center">@ViewBag.productName</h2>

    <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            @foreach (var item in Model)
            {
                <li data-target="#carousel-example-generic" data-slide-to=@item.Id></li>
            }
              
        </ol>
        <div class="carousel-inner">
            @foreach (var item in Model)
            {
                <div class="item">
                    <img style="width: 13%; height: 143px;" src="~/uploads/ProductImages/@item.ImagePath" title="@item.Title"  alt="@item.Title">
                </div>
            }       
        </div>
        
        <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
        </a>
        <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
        </a>
    </div> 

<style>
    .item img {
        overflow: hidden;
        width: 100%;
    }
</style>
<script src="~/lib/jquery/dist/jquery.js"></script>

<script>
    $(function () {
        $(".item").first().addClass("active");
        $("ol.carousel-indicators").find("li").eq(0).addClass("active");
    })
</script>
